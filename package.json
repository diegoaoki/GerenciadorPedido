{
  "name": "integracao-multiplataforma",
  "version": "0.1.0",
  "private": true,
  "description": "Hub central para gerenciar produtos, estoque e pedidos em múltiplos marketplaces",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "api:dev": "npm run start:dev --workspace=@imp/api",
    "web:dev": "npm run dev --workspace=@imp/web",
    "dev": "npm run db:up && npm run api:dev"
  },
  "engines": {
    "node": ">=20"
  }
}
